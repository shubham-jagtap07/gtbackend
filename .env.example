    # Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=chai_admin_db
DB_PORT=5432
# For managed Postgres (Render/Heroku/etc.)
# Set DB_SSL=true to enable SSL.
# If your provider uses self-signed certs, set DB_SSL_REJECT_UNAUTHORIZED=false
# Otherwise omit or set to true.
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=true

# Server Configuration
PORT=5001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=graduate_chai_super_secret_jwt_key_2024_make_it_long_and_random_for_security
JWT_EXPIRES_IN=24h

# Admin Credentials
ADMIN_EMAIL=admin@chaiwala.com
ADMIN_PASSWORD=admin123

# CORS Configuration
FRONTEND_URL=http://localhost:3000
ADMIN_PANEL_URL=http://localhost:3000
